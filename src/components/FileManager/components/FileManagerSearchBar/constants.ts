/*
 * The bar sits inside the grid card, above the table, so its padding is the
 * card's inner padding rather than a gap in the content column.
 */
export const searchBarBaseClassName = 'flex w-full shrink-0 items-center gap-3';

export const searchInputContainerClassName = 'min-w-0 flex-1';
